{% comment %}
  Store Logos Section - Exact copy from React StoreLogosWow.jsx
{% endcomment %}

<section class="py-12 sm:py-20 relative overflow-hidden">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 relative z-10">
    <!-- Header -->
    <div class="text-center mb-8 sm:mb-12">
      <div class="inline-flex items-center gap-2 glass rounded-full px-3 sm:px-4 py-2 mb-4 sm:mb-6 border border-emerald-500/20">
        <span class="w-2 h-2 bg-emerald-500 rounded-full animate-pulse"></span>
        <span class="text-emerald-500 text-xs sm:text-sm font-medium">{{ 'store_logos.badge' | t }}</span>
      </div>
      <h2 class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-bold text-white mb-3 sm:mb-4">
        {{ 'store_logos.title_1' | t }} <span class="text-transparent bg-clip-text bg-gradient-to-r from-purple-400 via-pink-500 to-orange-500">{{ 'store_logos.title_highlight' | t }}</span> {{ 'store_logos.title_2' | t }}
      </h2>
      <p class="text-gray-400 text-sm sm:text-lg max-w-2xl mx-auto">
        {{ 'store_logos.subtitle' | t }}
      </p>
    </div>

    <!-- Scrolling logos container -->
    <div class="relative">
      <div class="absolute left-0 top-0 bottom-0 w-16 sm:w-32 bg-gradient-to-r from-[#0a0a0b] to-transparent z-10 pointer-events-none"></div>
      <div class="absolute right-0 top-0 bottom-0 w-16 sm:w-32 bg-gradient-to-l from-[#0a0a0b] to-transparent z-10 pointer-events-none"></div>

      <!-- Scrolling row -->
      <div class="flex gap-4 sm:gap-6 overflow-hidden py-4" id="store-logos-scroll">
        {% assign stores = "TechStore,FashionHub,ElectroWorld,BeautyPro,SportMax,HomeStyle,GadgetWorld,FoodMarket,MegaStore,TrendyRu,FashionUA,TechnoKyiv,КазахТекстиль,AlmatyShop,TechBY,MinskFashion,UzFood,TashkentTech,BakuElectro,TbilisiShop" | split: "," %}
        {% assign domains = "techstore.ru,fashionhub.ru,electroworld.ru,beautypro.ru,sportmax.ru,homestyle.ru,gadgetworld.ru,foodmarket.ru,megastore.ru,trendyru.ru,fashionua.com.ua,technokyiv.ua,kazakhtextile.kz,almatyshop.kz,techby.by,minskfashion.by,uzfood.uz,tashkenttech.uz,bakuelectro.az,tbilisishop.ge" | split: "," %}
        {% assign flags = "🇷🇺,🇷🇺,🇷🇺,🇷🇺,🇷🇺,🇷🇺,🇷🇺,🇷🇺,🇷🇺,🇷🇺,🇺🇦,🇺🇦,🇰🇿,🇰🇿,🇧🇾,🇧🇾,🇺🇿,🇺🇿,🇦🇿,🇬🇪" | split: "," %}
        {% assign logos = "TS,FH,EW,BP,SM,HS,GW,FM,MS,TR,FU,TK,КТ,AS,TB,MF,UF,TT,BE,TS" | split: "," %}
        
        {% for j in (1..2) %}
          {% for i in (0..19) %}
            {% assign variant = i | modulo: 2 %}
            <div class="flex-shrink-0 group">
              <div class="w-48 sm:w-52 h-20 sm:h-24 rounded-xl glass border border-white/5 flex items-center gap-2 sm:gap-3 px-3 sm:px-4 transition-all duration-300 {% if variant == 0 %}hover:border-purple-500/30 hover:bg-purple-500/5{% else %}hover:border-orange-500/30 hover:bg-orange-500/5{% endif %} cursor-pointer">
                <div class="w-10 h-10 sm:w-12 sm:h-12 rounded-lg {% if variant == 0 %}bg-gradient-to-br from-purple-500/20 to-orange-500/20{% else %}bg-gradient-to-br from-orange-500/20 to-pink-500/20{% endif %} flex items-center justify-center border border-white/10 transition-colors">
                  <span class="text-white font-bold text-xs sm:text-sm">{{ logos[i] }}</span>
                </div>
                <div class="flex-1 min-w-0">
                  <p class="text-white font-medium text-xs sm:text-sm truncate">{{ stores[i] }}</p>
                  <p class="text-gray-500 text-[10px] sm:text-xs truncate">{{ domains[i] }}</p>
                  <div class="mt-1">
                    <span class="text-sm">{{ flags[i] }}</span>
                  </div>
                </div>
              </div>
            </div>
          {% endfor %}
        {% endfor %}
      </div>
    </div>

    <!-- Country flags summary -->
    <div class="grid grid-cols-4 md:grid-cols-8 gap-2 sm:gap-4 mt-8 sm:mt-12">
      {% assign country_flags = "ru,ua,kz,by,uz,az,ge,am" | split: "," %}
      {% assign country_names = "Россия,Украина,Казахстан,Беларусь,Узбекистан,Азербайджан,Грузия,Армения" | split: "," %}
      {% assign country_counts = "3200+,1500+,800+,450+,250+,180+,80+,40+" | split: "," %}
      {% assign country_colors = "from-red-500/20 to-blue-500/20,from-blue-500/20 to-yellow-500/20,from-cyan-500/20 to-yellow-500/20,from-red-500/20 to-green-500/20,from-blue-500/20 to-green-500/20,from-blue-500/20 to-red-500/20,from-red-500/20 to-white/10,from-red-500/20 to-orange-500/20" | split: "," %}
      {% assign country_borders = "hover:border-red-500/50,hover:border-blue-500/50,hover:border-cyan-500/50,hover:border-green-500/50,hover:border-blue-500/50,hover:border-red-500/50,hover:border-red-500/50,hover:border-orange-500/50" | split: "," %}
      
      {% for i in (0..7) %}
        <div class="glass rounded-xl sm:rounded-2xl p-2 sm:p-4 border border-white/10 {{ country_borders[i] }} transition-all duration-300 cursor-pointer group hover:scale-105">
          <div class="flex flex-col items-center text-center">
            <div class="w-10 h-10 sm:w-16 sm:h-16 rounded-lg sm:rounded-xl bg-gradient-to-br {{ country_colors[i] }} flex items-center justify-center mb-2 sm:mb-3 overflow-hidden">
              <img 
                src="https://flagcdn.com/w80/{{ country_flags[i] }}.png" 
                alt="{{ country_names[i] }}"
                class="w-8 h-6 sm:w-12 sm:h-9 object-cover rounded shadow-lg"
                loading="lazy"
              >
            </div>
            <p class="text-white text-[10px] sm:text-sm font-medium mb-0.5 sm:mb-1 hidden sm:block">{{ country_names[i] }}</p>
            <p class="text-emerald-400 text-xs sm:text-lg font-bold">{{ country_counts[i] }}</p>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const scrollContainer = document.getElementById('store-logos-scroll');
  if (scrollContainer) {
    let scrollPosition = 0;
    const speed = 0.3;
    
    function animate() {
      scrollPosition += speed;
      if (scrollPosition >= scrollContainer.scrollWidth / 2) {
        scrollPosition = 0;
      }
      scrollContainer.scrollLeft = scrollPosition;
      requestAnimationFrame(animate);
    }
    
    const observer = new IntersectionObserver(([entry]) => {
      if (entry.isIntersecting) {
        requestAnimationFrame(animate);
        observer.disconnect();
      }
    }, { threshold: 0.1 });
    
    observer.observe(scrollContainer);
  }
});
</script>

{% schema %}
{
  "name": "Store Logos",
  "settings": []
}
{% endschema %}
