{% comment %}
  Services Section - Exact copy from React ServicesSection.jsx
{% endcomment %}

<section id="services" class="py-24 relative z-10">
  <!-- Section background with glass effect -->
  <div class="absolute inset-0 glass-strong opacity-50"></div>
  
  <div class="max-w-7xl mx-auto px-6 relative">
    <!-- Section Header -->
    <div class="text-center mb-12">
      <span class="text-orange-500 text-sm font-medium tracking-wider uppercase mb-4 block">
        {{ 'services.section_title' | t }}
      </span>
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-4">
        {{ 'services.title_1' | t }} <span class="text-orange-500 text-glow-orange">{{ 'services.title_highlight' | t }}</span> {{ 'services.title_2' | t }}
      </h2>
      <p class="text-gray-400 text-lg max-w-2xl mx-auto">
        {{ 'services.subtitle' | t }}
      </p>
    </div>

    <!-- Service Tabs - Scrollable -->
    <div class="flex gap-2 flex-wrap justify-center mb-12" id="service-tabs">
      {% assign service_titles = "Shopify магазин,Структура и дизайн,Платёжные системы,Оптимизация сайта,Таргетированная реклама,Доставка и отправка" | split: "," %}
      {% assign service_ids = "shopify,design,payments,optimization,advertising,shipping" | split: "," %}
      
      {% for i in (0..5) %}
        <button
          onclick="showService('{{ service_ids[i] }}')"
          data-testid="service-tab-{{ service_ids[i] }}"
          id="tab-{{ service_ids[i] }}"
          class="px-5 py-2.5 rounded-full text-sm font-medium transition-all duration-300 {% if i == 0 %}bg-gradient-to-r from-orange-500 to-orange-600 text-white shadow-lg shadow-orange-500/30{% else %}glass text-gray-400 hover:text-white hover:border-orange-500/30 border border-transparent{% endif %}"
        >
          {{ service_titles[i] }}
        </button>
      {% endfor %}
    </div>

    <!-- Service Content -->
    <div class="grid lg:grid-cols-2 gap-12 items-start">
      <!-- Left Content - Service Info -->
      {% assign service_subtitles = "Создаём магазины на Shopify с нуля,Создаём продающие интернет-магазины с нуля,Интеграция всех способов оплаты,Скорость и производительность,Привлекаем целевых клиентов,Логистика без головной боли" | split: "," %}
      {% assign service_prices = "от $2,500,от $3,000,от $500,от $800,от $1,500/мес,от $700" | split: "," %}
      {% assign service_icons = "shopping-cart,palette,credit-card,zap,target,truck" | split: "," %}
      {% assign service_descriptions = "Создаём профессиональные интернет-магазины на Shopify — ведущей e-commerce платформе в мире. От кастомного дизайна до полной настройки мы запускаем магазины готовые к продажам.,Мы создаём уникальные интернет-магазины с продуманной структурой и современным дизайном. Каждый элемент направлен на увеличение конверсии.,Интегрируем все популярные способы оплаты включая банковские карты электронные кошельки криптовалюты и системы рассрочки.,Комплексная оптимизация производительности вашего магазина. Ускоряем загрузку страниц до 1 секунды.,Создаём и управляем рекламными кампаниями которые приносят целевой трафик и продажи.,Полная автоматизация логистики вашего магазина. Интегрируем службы доставки настраиваем автоматический расчёт стоимости." | split: "," %}
      {% assign gift_titles = "СУПЕР-ПОДАРОК,ПОДАРОК,ПОДАРОК,ПОДАРОК,ПОДАРОК,ПОДАРОК" | split: "," %}
      {% assign gift_descriptions = "10 топовых товаров для продажи — исследование ниши + поставщики БЕСПЛАТНО,10 топовых шаблонов продающих страниц,Бесплатная настройка первых 3 платёжных систем,Бесплатный аудит скорости + 1 год CDN,20 готовых рекламных креативов + стратегия,Бесплатная интеграция 5 служб доставки" | split: "," %}
      {% assign gift_values = "$1,500,$500,$300,$400,$600,$450" | split: "," %}
      
      {% for i in (0..5) %}
        <div class="space-y-6 floating-card p-8 rounded-2xl service-content {% if i != 0 %}hidden{% endif %}" id="content-{{ service_ids[i] }}">
          <!-- Icon and title -->
          <div class="flex items-start gap-4">
            <div class="w-14 h-14 rounded-xl bg-gradient-to-br from-orange-500 to-orange-600 flex items-center justify-center shadow-lg shadow-orange-500/30">
              <i data-lucide="{{ service_icons[i] }}" class="w-7 h-7 text-white"></i>
            </div>
            <div>
              <h3 class="text-2xl md:text-3xl font-bold text-white leading-tight">
                {{ service_subtitles[i] }}
              </h3>
              <p class="text-orange-500 text-sm font-medium mt-1">
                {{ service_prices[i] }}
              </p>
            </div>
          </div>
          
          <p class="text-gray-400 text-lg leading-relaxed">
            {{ service_descriptions[i] }}
          </p>

          <!-- Gift Banner -->
          <div class="relative overflow-hidden rounded-xl p-4 bg-gradient-to-r from-emerald-500/10 to-emerald-500/5 border border-emerald-500/30">
            <div class="absolute top-0 right-0 w-20 h-20 bg-emerald-500/20 rounded-full blur-2xl"></div>
            <div class="flex items-start gap-3 relative">
              <div class="w-10 h-10 rounded-lg bg-emerald-500/20 flex items-center justify-center flex-shrink-0">
                <i data-lucide="gift" class="w-5 h-5 text-emerald-500"></i>
              </div>
              <div>
                <div class="text-emerald-500 font-bold text-sm">🎁 {{ gift_titles[i] }}</div>
                <div class="text-white text-sm">{{ gift_descriptions[i] }}</div>
                <div class="text-gray-500 text-xs mt-1">Стоимость: {{ gift_values[i] }}</div>
              </div>
            </div>
          </div>
        </div>
      {% endfor %}

      <!-- Right Content - Features -->
      <div class="space-y-4">
        {% assign all_features = "Разработка на Shopify/Shopify Plus|Кастомная тема или Premium шаблон|Настройка платежей и доставки|Интеграция с CRM и ERP|Мобильная оптимизация|SEO-настройка магазина,UX/UI дизайн интернет-магазина|Адаптивный дизайн для всех устройств|Разработка фирменного стиля|Создание продающих лендингов|Дизайн email-шаблонов|Оптимизация пользовательского пути,Stripe PayPal Square|Криптовалютные платежи|Apple Pay Google Pay|Рассрочка и кредитование|Локальные платёжные системы|Защита от мошенничества,Оптимизация скорости загрузки|Сжатие изображений|Кэширование и CDN|Минификация кода|Lazy loading|Core Web Vitals оптимизация,Facebook & Instagram Ads|Google Ads & Shopping|TikTok Advertising|Ретаргетинг кампании|Look-alike аудитории|A/B тестирование креативов,Интеграция с курьерскими службами|Автоматический расчёт стоимости|Печать этикеток|Трекинг отправлений|Dropshipping интеграции|Fulfillment центры" | split: "," %}
        
        {% for i in (0..5) %}
          {% assign features = all_features[i] | split: "|" %}
          <div class="features-list {% if i != 0 %}hidden{% endif %}" id="features-{{ service_ids[i] }}">
            {% for feature in features %}
              <div
                class="flex items-center gap-4 p-4 rounded-xl floating-card hover:border-orange-500/30 transition-all duration-300 group cursor-pointer card-shine mb-4"
                style="animation-delay: {{ forloop.index | times: 0.1 }}s"
              >
                <div class="w-2 h-2 rounded-full bg-gradient-to-r from-orange-500 to-emerald-500 group-hover:scale-150 transition-transform"></div>
                <span class="text-white text-lg group-hover:text-orange-500 group-hover:translate-x-1 transition-all duration-300">
                  {{ feature }}
                </span>
              </div>
            {% endfor %}
          </div>
        {% endfor %}

        <!-- Link Button -->
        <a
          href="/pages/services"
          data-testid="services-view-all"
          class="inline-flex items-center gap-2 mt-6 text-orange-500 hover:text-white font-medium transition-colors duration-300 group"
        >
          <span class="neon-underline">
            {{ 'services.view_all' | t }}
          </span>
          <i data-lucide="arrow-right" class="w-5 h-5 group-hover:translate-x-2 transition-transform duration-300"></i>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .neon-underline {
    position: relative;
  }
  .neon-underline::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, #f97316, #10b981);
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.3s ease;
  }
  .group:hover .neon-underline::after,
  a:hover .neon-underline::after {
    transform: scaleX(1);
    transform-origin: left;
  }
</style>

<script>
function showService(serviceId) {
  // Hide all content and features
  document.querySelectorAll('.service-content').forEach(el => el.classList.add('hidden'));
  document.querySelectorAll('.features-list').forEach(el => el.classList.add('hidden'));
  
  // Reset all tabs
  document.querySelectorAll('#service-tabs button').forEach(btn => {
    btn.className = 'px-5 py-2.5 rounded-full text-sm font-medium transition-all duration-300 glass text-gray-400 hover:text-white hover:border-orange-500/30 border border-transparent';
  });
  
  // Show selected content and features
  document.getElementById('content-' + serviceId).classList.remove('hidden');
  document.getElementById('features-' + serviceId).classList.remove('hidden');
  
  // Highlight active tab
  document.getElementById('tab-' + serviceId).className = 'px-5 py-2.5 rounded-full text-sm font-medium transition-all duration-300 bg-gradient-to-r from-orange-500 to-orange-600 text-white shadow-lg shadow-orange-500/30';
  
  // Re-initialize lucide icons
  if (typeof lucide !== 'undefined') {
    lucide.createIcons();
  }
}
</script>

{% schema %}
{
  "name": "Services",
  "settings": []
}
{% endschema %}
