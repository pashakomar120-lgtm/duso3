<!-- Main Page Section -->
<section class="pt-24 sm:pt-32 pb-16 relative overflow-hidden min-h-screen">
  <div class="container">
    <article class="page">
      <header class="page-header mb-8 text-center">
        <h1 class="hero-title text-white mb-4">{{ page.title }}</h1>
      </header>
      <div class="text-gray-300 max-w-4xl mx-auto" style="line-height:1.8">{{ page.content }}</div>
    </article>
  </div>
</section>

{% schema %}
{
  "name": "Main Page",
  "settings": []
}
{% endschema %}
