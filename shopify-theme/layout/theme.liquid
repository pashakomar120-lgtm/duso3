<!DOCTYPE html>
<html lang="ru" class="scroll-smooth">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#0a0a0b">
  
  <title>{{ page_title | default: 'duso_ecom' }} | #1 Shopify Partner в СНГ</title>
  <meta name="description" content="{{ page_description | default: 'Создаём Shopify магазины, которые продают по всему СНГ. 6500+ успешных проектов.' }}">
  
  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
  
  <!-- TailwindCSS -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  
  <!-- Theme CSS -->
  <link rel="stylesheet" href="{{ 'theme.css' | asset_url }}">
  
  {{ content_for_header }}
</head>
<body class="antialiased" style="background-color: #0a0a0b; min-height: 100vh;">
  
  <!-- Background gradient effects -->
  <div class="bg-glow-wrapper">
    <div class="bg-glow-orange"></div>
    <div class="bg-glow-emerald"></div>
    <div class="bg-glow-center"></div>
  </div>
  
  <!-- Main content wrapper -->
  <div style="position: relative; z-index: 10;">
    {% section 'header' %}
    
    <main>
      {{ content_for_layout }}
    </main>
    
    {% section 'footer' %}
    {% section 'ai-assistant' %}
  </div>
  
  <!-- Lucide Icons -->
  <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js"></script>
  
  <!-- Theme JS -->
  <script src="{{ 'theme.js' | asset_url }}"></script>
</body>
</html>
